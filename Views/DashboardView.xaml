<UserControl x:Class="BhavaniSupportSuite.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:views="clr-namespace:BhavaniSupportSuite.Views"
             mc:Ignorable="d"
             Foreground="{StaticResource TextPrimaryBrush}">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <Grid Margin="4">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- System Overview Cards -->
            <UniformGrid Grid.Row="0" Columns="4" Margin="0,0,0,16">
                <Border Style="{StaticResource CardBorder}" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="HOSTNAME" Style="{StaticResource StatusText}" Margin="0,0,0,6" />
                        <TextBlock Text="{Binding Hostname}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18" FontWeight="Bold" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="4,0,4,0">
                    <StackPanel>
                        <TextBlock Text="OPERATING SYSTEM" Style="{StaticResource StatusText}" Margin="0,0,0,6" />
                        <TextBlock Text="{Binding OsBuild}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="14" FontWeight="SemiBold" TextWrapping="Wrap" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="4,0,4,0">
                    <StackPanel>
                        <TextBlock Text="INSTALLED RAM" Style="{StaticResource StatusText}" Margin="0,0,0,6" />
                        <TextBlock Text="{Binding InstalledRam}" Foreground="{StaticResource AccentBrush}" FontSize="18" FontWeight="Bold" />
                    </StackPanel>
                </Border>
                <Border Style="{StaticResource CardBorder}" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="UPTIME" Style="{StaticResource StatusText}" Margin="0,0,0,6" />
                        <TextBlock Text="{Binding Uptime}" Foreground="{StaticResource AccentBrush}" FontSize="18" FontWeight="Bold" />
                    </StackPanel>
                </Border>
            </UniformGrid>

            <!-- Live Telemetry -->
            <Border Grid.Row="1" Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="Live System Telemetry" Style="{StaticResource SectionTitle}" />
                    <UniformGrid Columns="5">
                        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="14" Margin="0,0,6,0">
                            <StackPanel>
                                <TextBlock Text="CPU Usage" Style="{StaticResource StatusText}" />
                                <TextBlock Text="{Binding CpuUsage, StringFormat={}{0:F1}%}" Foreground="{StaticResource AccentBrush}" FontSize="22" FontWeight="Bold" Margin="0,6,0,0" />
                                <ProgressBar Value="{Binding CpuUsage}" Maximum="100" Margin="0,8,0,0" Height="4" />
                            </StackPanel>
                        </Border>
                        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="14" Margin="3,0">
                            <StackPanel>
                                <TextBlock Text="Memory" Style="{StaticResource StatusText}" />
                                <TextBlock Text="{Binding MemoryUsage, StringFormat={}{0:F1}%}" Foreground="{StaticResource AccentBrush}" FontSize="22" FontWeight="Bold" Margin="0,6,0,0" />
                                <ProgressBar Value="{Binding MemoryUsage}" Maximum="100" Margin="0,8,0,0" Height="4" />
                            </StackPanel>
                        </Border>
                        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="14" Margin="3,0">
                            <StackPanel>
                                <TextBlock Text="CPU Temp" Style="{StaticResource StatusText}" />
                                <TextBlock Foreground="{StaticResource AccentBrush}" FontSize="22" FontWeight="Bold" Margin="0,6,0,0">
                                    <Run Text="{Binding CpuTemperature, StringFormat={}{0:F0}\u00B0C}"/>
                                    <Run Text=" / "/>
                                    <Run Text="{Binding CpuTemperatureF, StringFormat={}{0:F0}\u00B0F}"/>
                                </TextBlock>
                            </StackPanel>
                        </Border>
                        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="14" Margin="3,0">
                            <StackPanel>
                                <TextBlock Text="Disk Read" Style="{StaticResource StatusText}" />
                                <TextBlock Text="{Binding DiskIO, StringFormat={}{0:F1} MB/s}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18" FontWeight="Bold" Margin="0,6,0,0" />
                            </StackPanel>
                        </Border>
                        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="8" Padding="14" Margin="6,0,0,0">
                            <StackPanel>
                                <TextBlock Text="Network" Style="{StaticResource StatusText}" />
                                <TextBlock Text="{Binding NetworkThroughput, StringFormat={}{0:F1} MB/s}" Foreground="{StaticResource TextPrimaryBrush}" FontSize="18" FontWeight="Bold" Margin="0,6,0,0" />
                            </StackPanel>
                        </Border>
                    </UniformGrid>
                </StackPanel>
            </Border>

            <!-- System Details & Repair -->
            <Grid Grid.Row="2" Margin="0,12,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource CardBorder}" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="System Information" Style="{StaticResource SectionTitle}" />
                        <ListBox ItemsSource="{Binding SystemInfo}" Background="Transparent" BorderThickness="0" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12" />
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Style="{StaticResource CardBorder}" Margin="8,0,0,0">
                    <StackPanel>
                        <TextBlock Text="One-Click First-Touch Fix" Style="{StaticResource SectionTitle}" />
                        <TextBlock Text="SFC, DISM, WinSock Reset, DNS Flush - sequential with live log." Style="{StaticResource StatusText}" Margin="0,0,0,12" />

                        <ProgressBar Value="{Binding IsRepairRunning, Converter={x:Static views:BoolToProgressConverter.Instance}}" Maximum="1" Margin="0,0,0,12" Height="4" />

                        <Button Content="Run System Repair" Command="{Binding RunRepairCommand}" Style="{StaticResource AccentButtonStyle}" IsEnabled="{Binding IsRepairRunning, Converter={x:Static views:InverseBoolConverter.Instance}}" HorizontalAlignment="Left" />

                        <TextBlock Text="{Binding RepairStatus}" Style="{StaticResource StatusText}" Margin="0,12,0,8" />

                        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="6" Padding="12" MaxHeight="200">
                            <ScrollViewer VerticalScrollBarVisibility="Auto">
                                <ItemsControl ItemsSource="{Binding RepairLog}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding}" Foreground="{StaticResource TextSecondaryBrush}" FontFamily="Consolas" FontSize="11" TextWrapping="Wrap" Margin="0,1" />
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </ScrollViewer>
                        </Border>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </ScrollViewer>
</UserControl>
